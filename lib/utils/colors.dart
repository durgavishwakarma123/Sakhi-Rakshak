import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFD32F2F); // Red SOS accent
  static const Color primaryDark = Color(0xFF9A0007);
  static const Color secondary = Color(0xFFE91E63); // Pink safety accents
  static const Color accent = Color(0xFFFFC107); // Warm amber
  static const Color background = Color(0xFF0F0E13); // Deep premium space dark background
  static const Color surface = Color(0xFF1B1A23); // Elevated glassmorphic surfaces
  static const Color cardBg = Color(0xFF272535);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white30;
  static const Color border = Color(0xFF2E2C3D);
}